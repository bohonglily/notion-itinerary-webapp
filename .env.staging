# Staging Environment Variables
# This file contains staging-specific environment variables

# Admin Panel (Staging)
VITE_ADMIN_PASSWORD=staging_admin_password_change_me

# AI Configuration (Staging)
# Use development/testing AI providers or lower-tier services
VITE_AI_PROVIDER=gemini

# Development Configuration
NODE_ENV=staging
VITE_ENV=staging

# Debugging (Enable more verbose logging in staging)
VITE_DEBUG=true

# Notes for Staging:
# - Use separate API keys for staging to avoid affecting production quotas
# - Test with different AI providers to ensure fallback mechanisms work
# - Keep admin password different from production
# - Enable debug logging for better troubleshooting